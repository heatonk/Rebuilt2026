/**
 * FRC5010Lib — deprecated.
 *
 * <p>The {@code frc.robot.*} application no longer depends on anything under this package. The
 * tree is left in place as dead code for reference only. Do not re-import classes from
 * {@code org.frc5010.*} from {@code frc.robot.*} — the migration that removed those dependencies
 * (see {@code Remove Common Code Plan.md}) ended with {@code grep -r "org.frc5010"
 * src/main/java/frc/} returning zero matches, and that invariant should be preserved.
 *
 * <p>If you need behavior that previously came from here, port it into {@code
 * frc.robot.rebuilt.util} or a sibling package instead.
 *
 * @version 1.0.0
 */
package org.frc5010;
